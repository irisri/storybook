import { Meta } from '@storybook/blocks';
import { ColorPalette, ColorItem } from '@storybook/blocks';
import { colors } from '../components/colors.ts';

<Meta title='Colors' />

<ColorPalette>
  <ColorItem title='theme.color.greyPalette' subtitle='main color is 500' colors={colors.getColorForPlatter('Gray')} />
  <ColorItem title='theme.color.purplePalette' subtitle='main is 500' colors={colors.getColorForPlatter('Purple')} />
  <ColorItem title='theme.color.greenPalette' subtitle='main is 500' colors={colors.getColorForPlatter('Green')} />
  <ColorItem title='theme.color.redPalette' subtitle='main is 500' colors={colors.getColorForPlatter('Red')} />
  <ColorItem title='theme.color.redPalette' subtitle='main is 500' colors={colors.getColorForPlatter('Yellow')} />
</ColorPalette>
